import styled from 'styled-components';

import { gaps } from 'styles/vars';

export const StarsRatingWrapper = styled.div`
  display: flex;
  width: auto;
  padding: 0 ${gaps.small} ${gaps.xsmall} 0;
`;

StarsRatingWrapper.displayName = 'StarsRatingWrapper';
